# Autopilot Context Snapshot: Fix Trading 212 API

- Task seed: `$autopilot In this portfolio tracker, without breaking anything, fix the trading 212 API implementation to work in this`
- Original task status: activation-prompt
- Desired outcome: make the existing Trading 212 broker connection and sync flow work against the current Trading 212 Public API without regressing the rest of the portfolio tracker.
- Constraints: preserve existing encrypted-token storage, keep tokens server-side only, avoid destructive changes, keep the UI flow simple, and verify with focused tests plus the repo's available checks.
- Known repo evidence:
  - Trading 212 connector is isolated in `src/lib/brokers/trading212.ts`.
  - Broker connect/sync routes store one encrypted credential string and call the connector via `BrokerConnector`.
  - Brokers page has a single password input currently labelled API key.
  - There are no Trading 212-specific unit tests yet.
- External evidence:
  - Current Trading 212 docs list account summary at `GET /api/v0/equity/account/summary`.
  - Current Trading 212 docs list positions at `GET /api/v0/equity/positions`.
  - Current docs prefer HTTP Basic auth using API key as username and API secret as password, while legacy `Authorization` API-key header is still present as a security option.
- Unknowns / resolved assumptions:
  - No real Trading 212 credentials are available locally, so live API verification is out of scope.
  - Preserve compatibility with existing stored legacy one-piece keys by continuing to send a raw `Authorization` header unless the stored credential clearly contains a key/secret pair or a prebuilt Basic header.
- Likely touchpoints:
  - `src/lib/brokers/trading212.ts`
  - `src/lib/validation.ts`
  - `src/app/(app)/brokers/page.tsx`
  - `README.md`
  - new focused tests for Trading 212 normalization/request behavior.
- Scope note: this snapshot captures the Autopilot activation prompt and discovered evidence, not guaranteed prior conversation context.
