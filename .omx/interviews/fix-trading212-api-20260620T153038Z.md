# Deep Interview Summary: Fix Trading 212 API

## Readiness

- Profile: quick brownfield intake inside Autopilot.
- Final ambiguity: 0.12 against a 0.30 quick threshold.
- Interview-complete rationale: repo inspection and current official docs resolved the material ambiguity; the remaining credential-format choice has a backward-compatible default.

## Requirements

- Fix Trading 212 connection and sync against the current public API.
- Keep credentials encrypted at rest and never returned to the browser.
- Support the current API key + API secret Basic-auth flow without breaking existing stored legacy one-piece API keys.
- Replace obsolete connector endpoints with documented current endpoints.
- Add focused regression tests so endpoint/auth/response-shape drift is caught.

## Non-goals

- Do not add order placement or trading.
- Do not change database schema unless unavoidable.
- Do not require users with existing legacy connections to reconnect unless the legacy API itself rejects them.
- Do not perform a live Trading 212 call because credentials are not available in this workspace.

## Decision Boundaries

- The agent may decide UI wording, validation limits, adapter types, and tests needed for the API compatibility fix.
- The agent may preserve one password field and document `API_KEY:API_SECRET` input to avoid schema churn.
- The agent may not remove existing encrypted credentials or run destructive database operations.

## Evidence

- `src/lib/brokers/trading212.ts` currently calls `/api/v0/equity/account/info`, `/api/v0/equity/portfolio`, and `/api/v0/equity/account/cash`.
- Current official docs list account summary at `/api/v0/equity/account/summary`.
- Current official docs list positions at `/api/v0/equity/positions`.
- Current official docs describe HTTP Basic auth with API key as username and API secret as password, while still listing a legacy API-key header option.

## Acceptance Criteria

- `Trading212Connector.testConnection` calls account summary and returns account currency/id from the current response shape.
- `Trading212Connector.fetchHoldings` calls positions and maps current response fields into `NormalizedHolding`.
- Connector request auth sends `Authorization: Basic <base64(API_KEY:API_SECRET)>` for key/secret credentials and preserves raw legacy header support.
- Cash balance comes from account summary cash fields.
- Trading 212 tests cover Basic auth, legacy auth, summary/positions response mapping, warnings on metadata failure, and ticker normalization.
- Existing test, lint, and typecheck commands pass or any environment-only blockers are documented.
